# 🔧 Настройка бота на Vercel (24/7)

## ⚠️ ВАЖНО: Бот должен работать через WEBHOOK, а не через polling!

## Шаг 1: Деплой на Vercel

1. Убедитесь, что проект задеплоен на Vercel:
   ```bash
   vercel --prod
   ```

2. Получите URL вашего проекта (например: `https://mafiachat.vercel.app`)

## Шаг 2: Установка переменных окружения на Vercel

1. Зайдите в Vercel Dashboard → ваш проект → Settings → Environment Variables
2. Добавьте:
   - `BOT_TOKEN` = `8266109869:AAGrCaaptlP-zVLDhULXT2btDCJEJE7PR1c`
   - `VERCEL_URL` = ваш URL (например: `https://mafiachat.vercel.app`)

## Шаг 3: Настройка Webhook

### Вариант 1: Через скрипт (рекомендуется)

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Создайте файл `.env`:
   ```
   BOT_TOKEN=8266109869:AAGrCaaptlP-zVLDhULXT2btDCJEJE7PR1c
   VERCEL_URL=https://your-project.vercel.app
   ```

3. Запустите скрипт:
   ```bash
   node setup-webhook.js
   ```

### Вариант 2: Вручную через API

Откройте в браузере или через curl:
```
https://api.telegram.org/bot8266109869:AAGrCaaptlP-zVLDhULXT2btDCJEJE7PR1c/setWebhook?url=https://your-project.vercel.app/api/bot
```

## Шаг 4: Проверка

1. Отправьте сообщение боту: `@FlowersMafia_help_bot`
2. Проверьте, что сообщение появилось в группе
3. Ответьте на сообщение через Reply
4. Клиент должен получить ответ

## 🔍 Проверка статуса webhook

Откройте в браузере:
```
https://api.telegram.org/bot8266109869:AAGrCaaptlP-zVLDhULXT2btDCJEJE7PR1c/getWebhookInfo
```

Должно быть:
- `url`: ваш Vercel URL
- `pending_update_count`: 0
- `last_error_date`: отсутствует

## ⚠️ Важные моменты

1. **НЕ запускайте `telegram-bot.js` локально** - это конфликтует с webhook!
2. **Webhook работает 24/7** - Vercel автоматически обрабатывает запросы
3. **Холодный старт**: Первый запрос может быть медленнее (1-2 сек), но это нормально
4. **Мониторинг**: Проверяйте логи в Vercel Dashboard → Functions → api/bot

## 🐛 Если бот не работает

1. Проверьте логи в Vercel Dashboard
2. Проверьте, что webhook установлен правильно
3. Убедитесь, что BOT_TOKEN правильный
4. Проверьте, что группа ID правильный: `-1003691437577`

## 📝 Как отвечать клиентам

1. Клиент пишет боту → сообщение появляется в группе
2. В группе нажмите **"Ответить" (Reply)** на сообщение от бота
3. Напишите ответ и отправьте
4. Бот автоматически отправит ответ клиенту!

Альтернатива: используйте команду `/send_USERID ваш ответ`


