# Инструкция по настройке Telegram бота @FlowersMafia_help_bot

## 📋 Что нужно сделать

### 1. Приветственное сообщение (Welcome Message)

Отправьте боту @BotFather следующее сообщение:

```
/setdescription
```

Затем выберите бота `@FlowersMafia_help_bot` и введите описание:

```
🌹 Цветочная Мафия | Flowers Mafia

Добро пожаловать! Я помогу вам:
• Узнать о доставке и ценах
• Оформить заказ
• Связаться с менеджерами
• Получить консультацию по букетам

Просто напишите ваш вопрос, и мы ответим в течение минуты! ⚡
```

---

### 2. Команды бота (Commands)

Отправьте боту @BotFather:

```
/setcommands
```

Выберите бота `@FlowersMafia_help_bot` и введите список команд:

```
start - Начать диалог с менеджером
help - Помощь и информация
delivery - Узнать о доставке
prices - Цены на букеты
pickup - Самовывоз (скидка 600₽)
order - Оформить заказ
contact - Связаться с менеджером
status - Статус заказа
```

---

### 3. About текст (Описание бота)

Отправьте @BotFather:

```
/setabouttext
```

Выберите бота и введите:

```
🌹 Цветочная Мафия - Букеты, от которых невозможно отказаться!
Доставка по Нижнему Новгороду и области. Круглосуточно.
Скидка 600₽ при самовывозе!
```

---

### 4. Настройка пересылки сообщений в группу

**ВАЖНО:** Для работы пересылки сообщений в группу нужен код бота. Это делается через Bot API или вебхуки.

### Вариант А: Через Bot API (рекомендуется)

Создайте файл `bot.js` для обработки сообщений:

```javascript
const TelegramBot = require('node-telegram-bot-api');
const token = 'ВАШ_ТОКЕН_БОТА'; // Получите у @BotFather через /token
const GROUP_ID = -1003691437577;

const bot = new TelegramBot(token, { polling: true });

// Обработка всех текстовых сообщений
bot.on('message', (msg) => {
    const chatId = msg.chat.id;
    const userId = msg.from.id;
    const userName = msg.from.first_name + (msg.from.last_name ? ' ' + msg.from.last_name : '');
    const username = msg.from.username ? '@' + msg.from.username : 'без username';
    const text = msg.text;

    // Игнорируем команды /start и служебные сообщения
    if (msg.text && msg.text.startsWith('/')) {
        if (msg.text === '/start') {
            bot.sendMessage(chatId, 
                `🌹 Добро пожаловать в Цветочную Мафию!\n\n` +
                `Я передам ваше сообщение менеджерам, и они ответят в течение минуты.\n\n` +
                `Просто напишите ваш вопрос или используйте команды:\n` +
                `/help - помощь\n` +
                `/delivery - доставка\n` +
                `/prices - цены\n` +
                `/order - заказ`
            );
        }
        return;
    }

    // Пересылаем сообщение в группу с информацией о пользователе
    if (text) {
        const messageToGroup = 
            `📩 Новое сообщение от клиента:\n\n` +
            `👤 Имя: ${userName}\n` +
            `🆔 ID: ${userId}\n` +
            `📱 Username: ${username}\n\n` +
            `💬 Сообщение:\n${text}\n\n` +
            `━━━━━━━━━━━━━━━━\n` +
            `Ответить: /reply_${userId}`;

        bot.sendMessage(GROUP_ID, messageToGroup, {
            reply_markup: {
                inline_keyboard: [[
                    { text: '📝 Ответить клиенту', callback_data: `reply_${userId}` }
                ]]
            }
        });

        // Подтверждение клиенту
        bot.sendMessage(chatId, '✅ Ваше сообщение отправлено менеджерам! Ответим в течение минуты.');
    }

    // Обработка фото/документов
    if (msg.photo) {
        const photoId = msg.photo[msg.photo.length - 1].file_id;
        bot.sendPhoto(GROUP_ID, photoId, {
            caption: `📷 Фото от ${userName} (${username})\nID: ${userId}`
        });
        bot.sendMessage(chatId, '✅ Фото отправлено менеджерам!');
    }
});

// Обработка ответов от менеджеров в группе
bot.on('callback_query', (query) => {
    if (query.data.startsWith('reply_')) {
        const userId = query.data.replace('reply_', '');
        // Здесь можно открыть диалог для ответа
        bot.answerCallbackQuery(query.id, { text: 'Используйте команду /send для отправки ответа' });
    }
});

// Команда для менеджеров: /send USER_ID текст ответа
bot.onText(/\/send (\d+) (.+)/, (msg, match) => {
    const userId = match[1];
    const replyText = match[2];
    
    // Проверяем, что команда отправлена из группы
    if (msg.chat.id === GROUP_ID) {
        bot.sendMessage(userId, `💬 Ответ от менеджера:\n\n${replyText}`);
        bot.sendMessage(GROUP_ID, `✅ Ответ отправлен клиенту ${userId}`);
    }
});

console.log('🤖 Бот запущен и готов к работе!');
```

---

### Вариант Б: Через вебхуки (для Vercel)

Если хотите использовать вебхуки вместо polling, можно создать API endpoint на Vercel.

---

## 📝 Команды для менеджеров в группе

Менеджеры могут отвечать клиентам двумя способами:

### Способ 1: Команда /send
```
/send USER_ID Ваш ответ клиенту
```

Пример:
```
/send 123456789 Спасибо за заказ! Доставим сегодня до 18:00.
```

### Способ 2: Ответ на сообщение (если настроить)
Можно настроить бота так, чтобы менеджеры отвечали на пересланное сообщение, и бот автоматически отправлял ответ клиенту.

---

## 🔧 Быстрый старт

1. Получите токен бота у @BotFather: `/token`
2. Установите зависимости: `npm install node-telegram-bot-api`
3. Создайте файл `bot.js` с кодом выше
4. Замените `ВАШ_ТОКЕН_БОТА` на реальный токен
5. Запустите: `node bot.js`

---

## ⚠️ Важные моменты

1. **Бот должен быть администратором группы** или иметь права на отправку сообщений
2. **GROUP_ID должен быть правильным** (с минусом для групп)
3. **Токен бота держите в секрете** - не публикуйте в репозитории
4. Для продакшена используйте переменные окружения (`.env`)

---

## 📱 Примеры сообщений от клиентов

Клиент пишет: "Хочу заказать букет роз"

Бот пересылает в группу:
```
📩 Новое сообщение от клиента:

👤 Имя: Иван Иванов
🆔 ID: 123456789
📱 Username: @ivanov

💬 Сообщение:
Хочу заказать букет роз

━━━━━━━━━━━━━━━━
Ответить: /reply_123456789
```

Менеджер отвечает:
```
/send 123456789 Здравствуйте! Какой букет вас интересует? У нас есть розы от 1500₽. Могу показать варианты!
```

Клиент получает:
```
💬 Ответ от менеджера:

Здравствуйте! Какой букет вас интересует? У нас есть розы от 1500₽. Могу показать варианты!
```

