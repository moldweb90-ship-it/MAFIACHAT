# 🚀 Деплой на Vercel

## Шаг 1: Подготовка

Проект уже загружен на GitHub: https://github.com/moldweb90-ship-it/MAFIACHAT.git

## Шаг 2: Подключение к Vercel

1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите "Add New Project"
4. Выберите репозиторий `moldweb90-ship-it/MAFIACHAT`
5. Нажмите "Import"

## Шаг 3: Настройка переменных окружения

В настройках проекта Vercel добавьте переменную окружения:

**Key:** `BOT_TOKEN`  
**Value:** `8266109869:AAGrCaaptlP-zVLDhULXT2btDCJEJE7PR1c`

## Шаг 4: Настройка вебхука для бота

После деплоя на Vercel получите URL вашего проекта (например: `https://mafiachat.vercel.app`)

Затем установите вебхук для бота через Bot API:

```bash
curl -X POST "https://api.telegram.org/bot8266109869:AAGrCaaptlP-zVLDhULXT2btDCJEJE7PR1c/setWebhook?url=https://ваш-проект.vercel.app/api/bot"
```

Или используйте этот скрипт:

```javascript
const https = require('https');

const BOT_TOKEN = '8266109869:AAGrCaaptlP-zVLDhULXT2btDCJEJE7PR1c';
const WEBHOOK_URL = 'https://ваш-проект.vercel.app/api/bot';

const url = `https://api.telegram.org/bot${BOT_TOKEN}/setWebhook?url=${WEBHOOK_URL}`;

https.get(url, (res) => {
    let data = '';
    res.on('data', (chunk) => { data += chunk; });
    res.on('end', () => { console.log(data); });
}).on('error', (err) => { console.error(err); });
```

## Шаг 5: Проверка работы

1. Откройте вашего бота в Telegram: `@FlowersMafia_help_bot`
2. Отправьте команду `/start`
3. Напишите тестовое сообщение
4. Проверьте, что сообщение появилось в группе

## 📝 Структура проекта на Vercel

```
/
├── api/
│   └── bot.js          # Serverless функция для обработки вебхуков
├── index.html          # Чат-виджет
├── images/             # Изображения
├── package.json
└── vercel.json         # Конфигурация Vercel
```

## ⚠️ Важно

1. **Токен бота** должен быть в переменных окружения Vercel
2. **Вебхук** должен быть установлен после деплоя
3. **Бот должен быть в группе** с ID `-1003691437577`
4. Для локальной разработки используйте `telegram-bot.js` (polling)
5. Для продакшена на Vercel используйте `api/bot.js` (webhook)

## 🔧 Локальная разработка

Для локальной разработки бота:

```bash
npm run bot
```

Для локального сервера виджета:

```bash
npm start
```

## 📱 Чат-виджет

Чат-виджет будет доступен по адресу: `https://ваш-проект.vercel.app`

Для подключения на Joomla сайте добавьте в `<head>`:

```html
<script src="https://ваш-проект.vercel.app/widget.js" async></script>
```

(Файл `widget.js` можно создать позже для более удобного подключения)

